# Copyright (c) 2022-2026, The Isaac Lab Project Developers.
# All rights reserved.
#
# SPDX-License-Identifier: BSD-3-Clause

"""Task-specific observation functions for {{ task_name }}.

Add custom observation terms here. They will be available as `mdp.<func_name>`
in your task config's ObservationsCfg.

Base observations/helpers (get_eef_pos, get_eef_quat, get_object_pos,
get_proximal_joint_mean, to_tensor, object_obs, get_robot_joint_state,
get_all_robot_link_state) are already available from the base_il_env mdp.
{% if subtask_terms.funcs %}

This module also defines the **subtask termination signals** that the Mimic
data generation pipeline uses to slice a demonstration into reusable segments.
Each signal returns a ``(num_envs, 1)`` float tensor that is ``1.0`` when the
associated subtask is complete for that env and ``0.0`` otherwise.

The signals are wired into the Mimic recorder via the ``SubtaskTermsCfg``
observation group (group name ``subtask_terms``) in ``{{ task_name }}_cfg.py``,
read out by ``BaseILEnv.get_subtask_term_predicates``, and referenced by name
from ``{{ task_name }}_mimic_cfg.py``'s ``SubTaskConfig`` entries.

Known reference predicates (``grasp_brick_done`` / ``move_brick_done`` /
``release_brick_done`` / ``idle_done``) are generated with concrete bodies.
Other generated predicates return zeros until you fill in the task geometry.
{% endif %}
"""

from __future__ import annotations

from typing import TYPE_CHECKING

import torch

{% set implemented_subtask_funcs = ["grasp_brick_done", "move_brick_done", "release_brick_done", "idle_done"] %}
{% set implemented_funcs = subtask_terms.funcs | selectattr("name", "in", implemented_subtask_funcs) | list %}
{% if implemented_funcs %}
from {{ package_name }}.base_il_env.mdp.observations import (
    get_eef_pos,
    get_object_pos,
    get_proximal_joint_mean,
    to_tensor,
)

{% endif %}
if TYPE_CHECKING:
    from isaaclab.envs import ManagerBasedRLEnv


{% if subtask_terms.funcs %}
{% if implemented_funcs %}
# ---------------------------------------------------------------------------
# Debug helpers
# ---------------------------------------------------------------------------
# Throttle subtask predicate prints so they don't flood stdout at sim rate.
_DEBUG_SUBTASKS_ENV = "{{ task_name.upper() }}_DEBUG_SUBTASKS"
_DEBUG_DEFAULT_EVERY = 0
_print_counters: dict[str, int] = {}


def _check_mark(ok: bool) -> str:
    return "OK  " if ok else "FAIL"


def _debug_subtask(
    env,
    signal_name: str | None,
    header: str,
    rows: list[tuple[str, float, str, float, bool]],
) -> None:
    """Pretty-print a subtask predicate's evaluation, gated to the current queue head."""
    if signal_name is None:
        return
    heads = getattr(env, "_debug_subtask_heads", None)
    if heads is not None and signal_name not in heads:
        return

    import os

    raw = os.environ.get(_DEBUG_SUBTASKS_ENV, str(_DEBUG_DEFAULT_EVERY))
    try:
        every = int(raw)
    except ValueError:
        every = _DEBUG_DEFAULT_EVERY
    if every <= 0:
        return
    count = _print_counters.get(signal_name, 0) + 1
    _print_counters[signal_name] = count
    if count % every != 0:
        return

    label_width = max((len(label) for label, *_ in rows), default=0)
    lines = [header]
    for label, value, op, threshold, passed in rows:
        lines.append(
            f"    {label:<{label_width}}  {value:>7.3f}   need {op} {threshold:<7.3f}  {_check_mark(passed)}"
        )
    print("\n".join(lines), flush=True)


{% endif %}
# ---------------------------------------------------------------------------
# Subtask termination signals (subtask_terms observation group)
# ---------------------------------------------------------------------------
{% for fn in subtask_terms.funcs %}
{% if fn.name == "grasp_brick_done" %}
def grasp_brick_done(
    env: "ManagerBasedRLEnv",
    eef_link: str,
    object_name: str,
    dist_threshold: float,
    gripper_joint_pattern: str,
    gripper_closed_threshold: float,
    signal_name: str | None = None,
) -> torch.Tensor:
    """Fires when the EEF is near the object and the gripper is closed."""
    eef = get_eef_pos(env, eef_link)
    obj = get_object_pos(env, object_name)
    eef_obj_dist = torch.norm(eef - obj, dim=-1)
    grip = get_proximal_joint_mean(env, gripper_joint_pattern)
    near = eef_obj_dist <= dist_threshold
    closed = grip >= gripper_closed_threshold
    done = (near & closed).float().unsqueeze(-1)
    _debug_subtask(
        env,
        signal_name,
        f"[{signal_name}] done={_check_mark(bool(done[0, 0].item()))}",
        [
            (
                "eef → object distance",
                eef_obj_dist[0].item(),
                "≤",
                float(dist_threshold),
                bool(near[0].item()),
            ),
            ("gripper closure", grip[0].item(), "≥", float(gripper_closed_threshold), bool(closed[0].item())),
        ],
    )
    return done


{% elif fn.name == "move_brick_done" %}
def move_brick_done(
    env: "ManagerBasedRLEnv",
    eef_link: str,
    object_name: str,
    target_pos: tuple[float, float, float],
    eef_dist_threshold: float,
    target_dist_threshold: float,
    gripper_joint_pattern: str,
    gripper_closed_threshold: float,
    signal_name: str | None = None,
) -> torch.Tensor:
    """Fires when the EEF holds the object and the object is near target."""
    eef = get_eef_pos(env, eef_link)
    obj = get_object_pos(env, object_name)
    target = to_tensor(target_pos, env.device).expand_as(obj)
    eef_obj_dist = torch.norm(eef - obj, dim=-1)
    obj_tgt_dist = torch.norm(obj - target, dim=-1)
    grip = get_proximal_joint_mean(env, gripper_joint_pattern)
    near_eef = eef_obj_dist <= eef_dist_threshold
    near_target = obj_tgt_dist <= target_dist_threshold
    closed = grip >= gripper_closed_threshold
    done = (near_eef & near_target & closed).float().unsqueeze(-1)
    _debug_subtask(
        env,
        signal_name,
        f"[{signal_name}] done={_check_mark(bool(done[0, 0].item()))}",
        [
            (
                "eef → object distance",
                eef_obj_dist[0].item(),
                "≤",
                float(eef_dist_threshold),
                bool(near_eef[0].item()),
            ),
            (
                "object → target distance",
                obj_tgt_dist[0].item(),
                "≤",
                float(target_dist_threshold),
                bool(near_target[0].item()),
            ),
            ("gripper closure", grip[0].item(), "≥", float(gripper_closed_threshold), bool(closed[0].item())),
        ],
    )
    return done


{% elif fn.name == "release_brick_done" %}
def release_brick_done(
    env: "ManagerBasedRLEnv",
    object_name: str,
    target_pos: tuple[float, float, float],
    target_dist_threshold: float,
    gripper_joint_pattern: str,
    gripper_open_threshold: float,
    signal_name: str | None = None,
) -> torch.Tensor:
    """Fires when the object is near target and the gripper is open."""
    obj = get_object_pos(env, object_name)
    target = to_tensor(target_pos, env.device).expand_as(obj)
    obj_tgt_dist = torch.norm(obj - target, dim=-1)
    grip = get_proximal_joint_mean(env, gripper_joint_pattern)
    near_target = obj_tgt_dist <= target_dist_threshold
    is_open = grip <= gripper_open_threshold
    done = (near_target & is_open).float().unsqueeze(-1)
    _debug_subtask(
        env,
        signal_name,
        f"[{signal_name}] done={_check_mark(bool(done[0, 0].item()))}",
        [
            (
                "object → target distance",
                obj_tgt_dist[0].item(),
                "≤",
                float(target_dist_threshold),
                bool(near_target[0].item()),
            ),
            ("gripper openness", grip[0].item(), "≤", float(gripper_open_threshold), bool(is_open[0].item())),
        ],
    )
    return done


{% elif fn.name == "idle_done" %}
def idle_done(
    env: "ManagerBasedRLEnv",
    eef_link: str,
    idle_pos: tuple[float, float, float],
    threshold: tuple[float, float, float],
    signal_name: str | None = None,
) -> torch.Tensor:
    """Fires when the EEF is inside an axis-aligned idle box."""
    eef = get_eef_pos(env, eef_link)
    target = to_tensor(idle_pos, env.device).expand_as(eef)
    thr = to_tensor(threshold, env.device)
    delta = (eef - target).abs()
    per_axis_ok = delta <= thr
    in_range = per_axis_ok.all(dim=-1)
    done = in_range.float().unsqueeze(-1)
    _debug_subtask(
        env,
        signal_name,
        f"[{signal_name}] done={_check_mark(bool(done[0, 0].item()))}",
        [
            (
                "|Δx| (eef − idle)",
                delta[0, 0].item(),
                "≤",
                float(threshold[0]),
                bool(per_axis_ok[0, 0].item()),
            ),
            (
                "|Δy| (eef − idle)",
                delta[0, 1].item(),
                "≤",
                float(threshold[1]),
                bool(per_axis_ok[0, 1].item()),
            ),
            (
                "|Δz| (eef − idle)",
                delta[0, 2].item(),
                "≤",
                float(threshold[2]),
                bool(per_axis_ok[0, 2].item()),
            ),
        ],
    )
    return done


{% else %}
def {{ fn.name }}(
    env: "ManagerBasedRLEnv",
{% for p in fn.params %}
{% if p.type %}
    {{ p.name }}: {{ p.type }},
{% else %}
    {{ p.name }},
{% endif %}
{% endfor %}
    signal_name: str | None = None,
) -> torch.Tensor:
    """TODO: implement the ``{{ fn.name }}`` subtask predicate.

    Should return a ``(num_envs, 1)`` tensor that is ``1.0`` when the subtask
    is complete and ``0.0`` otherwise.
    """
    return torch.zeros(env.num_envs, 1, device=env.device)


{% endif %}
{% endfor %}
{% else %}
# Example: uncomment and customize
# def my_custom_observation(env: ManagerBasedRLEnv) -> torch.Tensor:
#     """Compute a task-specific observation."""
#     return torch.zeros(env.num_envs, 1, device=env.device)
{% endif %}
